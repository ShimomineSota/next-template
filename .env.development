# Per-environment config. Non-secret placeholder values ship in the template.
# Add real secrets with `npx dotenvx set KEY value -f .env.<env>`; the pre-commit
# guard then forces `npm run env:encrypt` before they can be committed.
# All three files must carry the same keys (values differ).
NEXT_PUBLIC_ENVIRONMENT=development

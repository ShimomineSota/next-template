node_modules
# .env.{development,staging,production} are tracked (plaintext placeholders in
# the template; the `.env*` pre-commit guard forces `npm run env:encrypt`
# before real values can be committed). .env.keys / *.local never commit.
.env*
!.env.development
!.env.staging
!.env.production
.dev.vars*
.next
next-env.d.ts
/dist/
.vinext/
.wrangler/
worker-configuration.d.ts
.claude/
*.tsbuildinfo

## Panda Css
styled-system
styled-system-studio
node_modules
# Only .env.example is tracked by default. Commit the dotenvx-encrypted
# per-env files with an explicit `git add -f` (the `.env*` staged guard blocks
# them unless they're encrypted); after that they're tracked normally.
.env*
!.env.example
.dev.vars*
.next
next-env.d.ts
/dist/
.vinext/
.wrangler/
worker-configuration.d.ts
.claude/
*.tsbuildinfo

## Panda Css
styled-system
styled-system-studio